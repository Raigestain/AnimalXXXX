using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum LAYERS
{
    FLOOR = 9,
    ANIMALS,
    MATERIALS
};

public enum ANIMAL_STATES
{
    IDLE = 0,
    WALK,
    RUN,
    JUMP,
    EAT,
    RESET,
};